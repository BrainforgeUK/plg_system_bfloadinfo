System plugin which replaces escape sequences embedded in a page.

To use embed code like the following into article, module etc wherever required:
````
your pre-text {bfloadinfo label} your post-text
````